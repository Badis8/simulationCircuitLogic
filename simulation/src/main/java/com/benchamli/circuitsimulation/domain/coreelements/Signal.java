package com.benchamli.circuitsimulation.domain.coreelements;
// this is just a custom type that is easier to read, the Signal , its juste binaire passing or blocking

public enum Signal {
    PASSING, BLOCKING;
}